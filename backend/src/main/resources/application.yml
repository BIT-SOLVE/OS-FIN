spring:
  application:
    name: ufos-platform
  datasource:
    url: jdbc:postgresql://localhost:5432/ufos_platform
    username: ufos_user
    password: ufos_password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8081/realms/ufos
          jwk-set-uri: http://localhost:8081/realms/ufos/protocol/openid-connect/certs

server:
  port: 8080
  error:
    include-message: always

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

app:
  version: 0.1.0
  cors:
    allowed-origins: http://localhost:5173

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
